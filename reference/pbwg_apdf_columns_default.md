# Columns used for APDF extracts

Returns the default column set used by PBWG APDF queries. These fields
carry the identifiers, movement times and wake turbulence class needed
for the legacy airport-level summaries.

## Usage

``` r
pbwg_apdf_columns_default()
```

## Value

Character vector of APDF column names.
